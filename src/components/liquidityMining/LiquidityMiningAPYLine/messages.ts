import { defineMessages } from 'react-intl';

export default defineMessages({
  apr: 'APR',
  kDay: 'per day',
  tooltipText: `Participating in this Pret market gives rewards in {token} token. The APR is just a representation on how these rewards would be during a 1-year period`,
});
